name: Publish update

on:
  push:
    branches:
      - "master"

jobs:
  publish_update:
    type: update
    params:
      channel: production
      message: ${{ github.event.head_commit.message }}
